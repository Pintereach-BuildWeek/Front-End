// IMPORTS

import {
    LOGIN_START,
    LOGIN_SUCCESS,
    SIGNUP_START,
  } from "../actions";

// STATE

const initialState = {

  };

// REDUCER

const reducer = (state = initialState, action) => {

};

export default reducer;